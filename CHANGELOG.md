# Changelog

本项目的主要功能更新记录如下。

## 2026-05-14

### 新增

- 新增 3x-ui 3.0.1 API Token 支持，优先通过 Bearer Token 调用 `/panel/api` 接口。
- 新增 3x-ui 面板删除功能。
- 新增 LDC 月度额度周期设置，支持按“每月某日 + 具体时间（北京时间）”重置。
- 新增后台 LDC 订单页的本周期额度占用和剩余统计展示。
- 新增 3x-ui 节点白名单的按面板分组搜索、多选和摘要展示。
- 新增“新增 3x-ui 面板”的折叠式录入卡片，减少后台设置页占用空间。

### 变更

- 3x-ui 客户端创建逻辑已切换到新版接口，优先使用：
  - `POST /panel/api/inbounds/addClient`
  - `POST /panel/api/inbounds/updateClient/:clientId`
- 当 API Token 不可用且同时配置了账号密码时，系统会自动回退到账号密码 + CSRF 登录模式。
- LDC 月度额度统计从“简单累计已支付订单”调整为“当前周期内仍有效的已支付/已完成订阅 + 短时待支付预占”。
- 跨周期但未到期的 LDC 订阅会继续占用新周期额度，到订阅到期后自动释放。
- Turnstile 前台渲染从多个标签页同时加载，调整为按当前激活标签页懒加载。
- 后台 3x-ui 设置页的节点选择交互改为更接近模型选择器的样式。

### 修复

- 修复 3x-ui 3.0.1 升级后旧登录接口或旧客户端创建方式导致的创建失败问题。
- 修复将错误内容误保存为 API Token 的问题，增加基础格式校验，避免把 URL、路径或明显无效值写入数据库。
- 修复 Turnstile 在隐藏标签页内多实例渲染时容易失败的问题。
- 修复服务端向 Cloudflare 传递 `127.0.0.1`、内网地址或保留地址时可能导致的人机验证失败问题。
- 修复后台“新增面板”展开后遮挡“保存 3x-ui 设置”按钮的问题，改为正常文档流布局，不再使用覆盖层。

### 删除保护与兼容说明

- 删除 3x-ui 面板时，至少保留一个面板。
- 已被兑换码、用户或 LDC 订单引用的面板不允许删除。
- 旧版账号密码登录方式仍然兼容，但推荐新版 3x-ui 面板统一改用 API Token。

### 文档

- 更新 `README.md`，补充 3x-ui API Token、LDC 月度周期、Turnstile 行为、面板删除限制等说明。

